# emails/
Branded HTML email helpers live in `lib/email/` (layout + templates). Waitlist thank-you: `lib/email/waitlist-thank-you.ts`. Product transactional emails in `server/services/notifications.ts` send plain text plus HTML via `plainTextEmailHtml`.
