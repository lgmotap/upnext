# Sprint 34 — Jobs list pagination

> Reuse sprint 32 list pattern on `/app/jobs`.

## Scope

- [x] Paginate jobs list (50 per page)
- [x] Total count in page header
- [x] Shared `ListPagination` component

## Validation

- [x] `npm run typecheck` + `npm run build`
