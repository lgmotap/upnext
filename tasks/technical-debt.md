# Technical Debt
- UI shell currently uses mock data in `lib/mock/` — replace with real data layer (server/repositories) in later sprints.
- shadcn/ui not yet installed; product primitives are hand-rolled in `components/app/` and `components/ui/`.
