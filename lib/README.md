# lib/
Shared infrastructure utilities and provider boundaries.
- `auth/` Supabase auth helpers · `db/` Prisma client · `stripe/` · `resend/` · `posthog/` · `sentry/`
- `dates/` UTC + timezone helpers · `money/` integer-cents helpers · `mock/` mock data for the UI shell · `utils/` misc.
