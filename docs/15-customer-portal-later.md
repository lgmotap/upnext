# Customer Portal (Later)

Out of MVP scope. Future v1 lets end customers view upcoming/past jobs, reschedule requests, see receipts, and pay outstanding balances via a magic-link portal (no password). Strictly read/limited-write, scoped to that customer's records.
